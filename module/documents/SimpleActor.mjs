/**
 * Extend the basic Actor
 * @extends {Actor}
 */
export default class SimpleActor extends Actor {
  /*  
  _onUpdate(changed, options, userId) { 
      return super._onUpdate(changed, options, userId)
    } */
  };